tasks/
__pycache__/
*.pyc
.DS_Store

# Local-only — kept on disk, not published to GitHub.
# The public repo carries the model pipelines, the simulation engines and
# the figure scripts: stephy/, CBLV-CNN/, simulate_and_extract/,
# simulate_and_extract_Denmark/, supportingFigures/.
batch_run/
CBLV-GAT/

# supportingFigures/ ships the scripts, not their rendered output.
supportingFigures/*.pdf
supportingFigures/*.png
supportingFigures/*.out

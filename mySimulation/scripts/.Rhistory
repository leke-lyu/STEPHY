library(treeio)
read.beast("/Users/lukelyu/Desktop/mySimulation/out/2_beast2.trees")
# Load required libraries
library(treeio)
library(ape)
library(dplyr)
tree_file <- "/Users/lukelyu/Desktop/mySimulation/out/0_beast2.trees"
# Read tree
tree <- read.beast(tree_file)
# Extract file prefix for graph_id
file_prefix <- sub("_beast2\\.trees$", "", basename(tree_file))
# Get pairwise distances between two types
get_pairwise_distances <- function(type1, type2, tip_info, patristic_dist) {
tips_type1 <- tip_info$tip_label[tip_info$type == type1]
tips_type2 <- tip_info$tip_label[tip_info$type == type2]
if (length(tips_type1) == 0 || length(tips_type2) == 0) {
return(numeric(0))
}
if (type1 == type2 && length(tips_type1) > 1) {
# Within-type distances
idx <- combn(length(tips_type1), 2)
patristic_dist[cbind(tips_type1[idx[1,]], tips_type1[idx[2,]])]
} else if (type1 != type2) {
# Between-type distances
as.vector(patristic_dist[tips_type1, tips_type2, drop = FALSE])
} else {
numeric(0)
}
}
names(tree)
state_name <- "STATE_0"
state_suffix <- sub("STATE_", "", state_name)
graph_id <- paste0(file_prefix, "_", state_suffix)
# Convert to phylo object
tree_phylo <- as.phylo(tree[[state_name]])
n_tips <- length(tree_phylo$tip.label)
# Extract tip information
tip_info <- tree[[state_name]]@data %>%
filter(node %in% 1:n_tips) %>%
arrange(as.numeric(node)) %>%
transmute(
tip_number = as.numeric(node),
tip_label = tree_phylo$tip.label[as.numeric(node)],
type = gsub("I\\{", "", type)
)
all_types <- sort(unique(tip_info$type))
# Calculate patristic distance matrix
patristic_dist <- cophenetic.phylo(tree_phylo)
# Generate all type combinations
type_combinations <- c(
lapply(all_types, function(t) c(t, t)),
if (length(all_types) > 1) combn(all_types, 2, simplify = FALSE) else list()
)
View(type_combinations)
# Calculate distances for each combination
csv_data <- do.call(rbind, lapply(type_combinations, function(combo) {
distances <- get_pairwise_distances(combo[1], combo[2], tip_info, patristic_dist)
if (length(distances) > 0) {
data.frame(
graph_id = graph_id,
src = combo[1],
dst = combo[2],
edge_features = paste0("[", paste(distances, collapse = ", "), "]"),
stringsAsFactors = FALSE
)
} else {
NULL
}
}))
View(csv_data)
library(treeio)
library(ape)
library(dplyr)
tree <- read.beast("/Users/lukelyu/Desktop/mySimulation/out/0_beast2.trees")
file_prefix <- sub("_beast2\\.trees$", "", basename(tree_file))
file_prefix <- sub("_beast2\\.trees$", "", basename("/Users/lukelyu/Desktop/mySimulation/out/0_beast2.trees"))
get_pairwise_distances <- function(type1, type2, tip_info, patristic_dist) {
tips_type1 <- tip_info$tip_label[tip_info$type == type1]
tips_type2 <- tip_info$tip_label[tip_info$type == type2]
if (length(tips_type1) == 0 || length(tips_type2) == 0) {
return(numeric(0))
}
if (type1 == type2 && length(tips_type1) > 1) {
idx <- combn(length(tips_type1), 2)
patristic_dist[cbind(tips_type1[idx[1,]], tips_type1[idx[2,]])]
} else if (type1 != type2) {
as.vector(patristic_dist[tips_type1, tips_type2, drop = FALSE])
} else {
numeric(0)
}
}
all_csv_data <- list()
names(tree)
state_name <- "STATE_2"
state_suffix <- sub("STATE_", "", state_name)
graph_id <- paste0(file_prefix, "_", state_suffix)
cat("  Converting to phylo object...\n")
tree_phylo <- as.phylo(tree[[state_name]])
n_tips <- length(tree_phylo$tip.label)
cat("  Found", n_tips, "tips\n")
if (n_tips == 0) {
cat("  Skipping - no tips\n")
next
}
cat("  Extracting tip information...\n")
tip_info <- tree[[state_name]]@data %>%
filter(node %in% 1:n_tips) %>%
arrange(as.numeric(node)) %>%
transmute(
tip_number = as.numeric(node),
tip_label = tree_phylo$tip.label[as.numeric(node)],
type = gsub("I\\{", "", type)
)
all_types <- sort(unique(tip_info$type))
cat("  Found", length(all_types), "types:", paste(all_types, collapse=", "), "\n")
if (length(all_types) == 0) {
cat("  Skipping - no types\n")
next
}
cat("  Calculating patristic distances...\n")
plot(tree_phylo)
View(tree_phylo)
tree_phylo[["edge"]]
has.singles(tree_phylo)
collapsed_tree <- collapse.singles(tree_phylo)
View(collapsed_tree)
View(tree_phylo)
plot(collapsed_tree)
has.singles(collapsed_tree)
tree_phylo[["edge"]]
library(treeio)
library(ape)
library(dplyr)
tree <- read.beast("/Users/lukelyu/Desktop/6_beast2.trees")
file_prefix <- sub("_beast2\\.trees$", "", basename(tree_file))
file_prefix <- sub("_beast2\\.trees$", "", basename("/Users/lukelyu/Desktop/6_beast2.trees"))
if (is.null(names(tree))) {
tree <- list(STATE_0 = tree)
}
get_pairwise_distances <- function(type1, type2, tip_info, patristic_dist) {
tips_type1 <- tip_info$tip_label[tip_info$type == type1]
tips_type2 <- tip_info$tip_label[tip_info$type == type2]
if (length(tips_type1) == 0 || length(tips_type2) == 0) {
return(numeric(0))
}
if (type1 == type2 && length(tips_type1) > 1) {
idx <- combn(length(tips_type1), 2)
patristic_dist[cbind(tips_type1[idx[1,]], tips_type1[idx[2,]])]
} else if (type1 != type2) {
as.vector(patristic_dist[tips_type1, tips_type2, drop = FALSE])
} else {
numeric(0)
}
}
all_csv_data <- list()
for (state_name in names(tree)) {
state_suffix <- sub("STATE_", "", state_name)
graph_id <- paste0(file_prefix, "_", state_suffix)
tree_phylo <- as.phylo(tree[[state_name]])
n_tips <- length(tree_phylo$tip.label)
if (n_tips == 0) {
next
}
tip_info <- tree[[state_name]]@data %>%
filter(node %in% 1:n_tips) %>%
arrange(as.numeric(node)) %>%
transmute(
tip_number = as.numeric(node),
tip_label = tree_phylo$tip.label[as.numeric(node)],
type = gsub("I\\{", "", type)
)
all_types <- sort(unique(tip_info$type))
if (length(all_types) == 0) {
next
}
patristic_dist <- cophenetic.phylo(tree_phylo)
type_combinations <- c(
lapply(all_types, function(t) c(t, t)),
if (length(all_types) > 1) combn(all_types, 2, simplify = FALSE) else list()
)
csv_data <- do.call(rbind, lapply(type_combinations, function(combo) {
distances <- get_pairwise_distances(combo[1], combo[2], tip_info, patristic_dist)
if (length(distances) > 0) {
data.frame(
graph_id = graph_id,
src = combo[1],
dst = combo[2],
edge_features = paste0("[", paste(distances, collapse = "\t"), "]"),
stringsAsFactors = FALSE
)
} else {
NULL
}
}))
all_csv_data[[length(all_csv_data) + 1]] <- csv_data
}
final_csv_data <- do.call(rbind, all_csv_data)
View(final_csv_data)
View(tip_info)
View(type_combinations)
View(patristic_dist)
View(tree_phylo)
getwd()
write.table(final_csv_data,
file = "my.csv",
sep = ",",
row.names = FALSE,
col.names = TRUE,
quote = FALSE)
tree <- read.beast("/Users/lukelyu/Desktop/mySimulation/multi/6_beast2.trees")
tree <- read.beast("/Users/lukelyu/Desktop/mySimulation/multi/6_beast2.trees")
file_prefix <- sub("_beast2\\.trees$", "", basename("/Users/lukelyu/Desktop/mySimulation/multi/6_beast2.trees"))
if (is.null(names(tree))) {
tree <- list(STATE_0 = tree)
}
get_pairwise_distances <- function(type1, type2, tip_info, patristic_dist) {
tips_type1 <- tip_info$tip_label[tip_info$type == type1]
tips_type2 <- tip_info$tip_label[tip_info$type == type2]
if (length(tips_type1) == 0 || length(tips_type2) == 0) {
return(numeric(0))
}
if (type1 == type2 && length(tips_type1) > 1) {
idx <- combn(length(tips_type1), 2)
patristic_dist[cbind(tips_type1[idx[1,]], tips_type1[idx[2,]])]
} else if (type1 != type2) {
as.vector(patristic_dist[tips_type1, tips_type2, drop = FALSE])
} else {
numeric(0)
}
}
all_csv_data <- list()
for (state_name in names(tree)) {
state_suffix <- sub("STATE_", "", state_name)
graph_id <- paste0(file_prefix, "_", state_suffix)
tree_phylo <- as.phylo(tree[[state_name]])
n_tips <- length(tree_phylo$tip.label)
if (n_tips == 0) {
next
}
tip_info <- tree[[state_name]]@data %>%
filter(node %in% 1:n_tips) %>%
arrange(as.numeric(node)) %>%
transmute(
tip_number = as.numeric(node),
tip_label = tree_phylo$tip.label[as.numeric(node)],
type = gsub("I\\{", "", type)
)
all_types <- sort(unique(tip_info$type))
if (length(all_types) == 0) {
next
}
patristic_dist <- cophenetic.phylo(tree_phylo)
type_combinations <- c(
lapply(all_types, function(t) c(t, t)),
if (length(all_types) > 1) combn(all_types, 2, simplify = FALSE) else list()
)
csv_data <- do.call(rbind, lapply(type_combinations, function(combo) {
distances <- get_pairwise_distances(combo[1], combo[2], tip_info, patristic_dist)
if (length(distances) > 0) {
data.frame(
graph_id = graph_id,
src = combo[1],
dst = combo[2],
edge_features = paste0("[", paste(distances, collapse = "\t"), "]"),
stringsAsFactors = FALSE
)
} else {
NULL
}
}))
all_csv_data[[length(all_csv_data) + 1]] <- csv_data
}
final_csv_data <- do.call(rbind, all_csv_data)
View(tip_info)
write.table(final_csv_data,
file = "my2.csv",
sep = ",",
row.names = FALSE,
col.names = TRUE,
quote = FALSE)
tree_file <- "/Users/lukelyu/Desktop/mySimulation/single/0_beast2.trees"
tree <- read.beast(tree_file)
library(treeio)
library(ape)
library(dplyr)
args <- commandArgs(trailingOnly = TRUE)
tree_file <- args[1]
output_file <- args[2]
tree_file <- "/Users/lukelyu/Desktop/mySimulation/single/0_beast2.trees"
tree <- read.beast(tree_file)
file_prefix <- sub("_beast2\\.trees$", "", basename(tree_file))
if (is.null(names(tree))) {
tree <- list(STATE_0 = tree)
}
get_pairwise_distances <- function(type1, type2, tip_info, patristic_dist) {
tips_type1 <- tip_info$tip_label[tip_info$type == type1]
tips_type2 <- tip_info$tip_label[tip_info$type == type2]
if (length(tips_type1) == 0 || length(tips_type2) == 0) {
return(numeric(0))
}
if (type1 == type2 && length(tips_type1) > 1) {
idx <- combn(length(tips_type1), 2)
patristic_dist[cbind(tips_type1[idx[1,]], tips_type1[idx[2,]])]
} else if (type1 != type2) {
as.vector(patristic_dist[tips_type1, tips_type2, drop = FALSE])
} else {
numeric(0)
}
}
all_csv_data <- list()
for (state_name in names(tree)) {
state_suffix <- sub("STATE_", "", state_name)
graph_id <- paste0(file_prefix, "_", state_suffix)
tree_phylo <- as.phylo(tree[[state_name]])
n_tips <- length(tree_phylo$tip.label)
if (n_tips == 0) {
next
}
tip_info <- tree[[state_name]]@data %>%
filter(node %in% 1:n_tips) %>%
arrange(as.numeric(node)) %>%
transmute(
tip_number = as.numeric(node),
tip_label = tree_phylo$tip.label[as.numeric(node)],
type = gsub("I\\{", "", type)
)
all_types <- sort(unique(tip_info$type))
if (length(all_types) == 0) {
next
}
patristic_dist <- cophenetic.phylo(tree_phylo)
type_combinations <- c(
lapply(all_types, function(t) c(t, t)),
if (length(all_types) > 1) combn(all_types, 2, simplify = FALSE) else list()
)
csv_data <- do.call(rbind, lapply(type_combinations, function(combo) {
distances <- get_pairwise_distances(combo[1], combo[2], tip_info, patristic_dist)
if (length(distances) > 0) {
sorted_distances <- sort(distances)
data.frame(
graph_id = graph_id,
src = combo[1],
dst = combo[2],
edge_features = paste0("[", paste(sorted_distances, collapse = "\t"), "]"),
stringsAsFactors = FALSE
)
} else {
NULL
}
}))
all_csv_data[[length(all_csv_data) + 1]] <- csv_data
}
View(all_csv_data)
library(treeio)
library(ape)
library(dplyr)
args <- commandArgs(trailingOnly = TRUE)
tree_file <- args[1]
output_file <- args[2]
tree_file <- "/Users/lukelyu/Desktop/mySimulation/multi/6_beast2.trees"
tree <- read.beast(tree_file)
file_prefix <- sub("_beast2\\.trees$", "", basename(tree_file))
if (is.null(names(tree))) {
tree <- list(STATE_0 = tree)
}
get_pairwise_distances <- function(type1, type2, tip_info, patristic_dist) {
tips_type1 <- tip_info$tip_label[tip_info$type == type1]
tips_type2 <- tip_info$tip_label[tip_info$type == type2]
if (length(tips_type1) == 0 || length(tips_type2) == 0) {
return(numeric(0))
}
if (type1 == type2 && length(tips_type1) > 1) {
idx <- combn(length(tips_type1), 2)
patristic_dist[cbind(tips_type1[idx[1,]], tips_type1[idx[2,]])]
} else if (type1 != type2) {
as.vector(patristic_dist[tips_type1, tips_type2, drop = FALSE])
} else {
numeric(0)
}
}
all_csv_data <- list()
for (state_name in names(tree)) {
state_suffix <- sub("STATE_", "", state_name)
graph_id <- paste0(file_prefix, "_", state_suffix)
tree_phylo <- as.phylo(tree[[state_name]])
n_tips <- length(tree_phylo$tip.label)
if (n_tips == 0) {
next
}
tip_info <- tree[[state_name]]@data %>%
filter(node %in% 1:n_tips) %>%
arrange(as.numeric(node)) %>%
transmute(
tip_number = as.numeric(node),
tip_label = tree_phylo$tip.label[as.numeric(node)],
type = gsub("I\\{", "", type)
)
all_types <- sort(unique(tip_info$type))
if (length(all_types) == 0) {
next
}
patristic_dist <- cophenetic.phylo(tree_phylo)
type_combinations <- c(
lapply(all_types, function(t) c(t, t)),
if (length(all_types) > 1) combn(all_types, 2, simplify = FALSE) else list()
)
csv_data <- do.call(rbind, lapply(type_combinations, function(combo) {
distances <- get_pairwise_distances(combo[1], combo[2], tip_info, patristic_dist)
if (length(distances) > 0) {
sorted_distances <- sort(distances)
data.frame(
graph_id = graph_id,
src = combo[1],
dst = combo[2],
edge_features = paste0("[", paste(sorted_distances, collapse = "\t"), "]"),
stringsAsFactors = FALSE
)
} else {
NULL
}
}))
all_csv_data[[length(all_csv_data) + 1]] <- csv_data
}
names(tree)
state_name <- "STATE_1"
state_suffix <- sub("STATE_", "", state_name)
graph_id <- paste0(file_prefix, "_", state_suffix)
tree_phylo <- as.phylo(tree[[state_name]])
n_tips <- length(tree_phylo$tip.label)
if (n_tips == 0) {
next
}
tip_info <- tree[[state_name]]@data %>%
filter(node %in% 1:n_tips) %>%
arrange(as.numeric(node)) %>%
transmute(
tip_number = as.numeric(node),
tip_label = tree_phylo$tip.label[as.numeric(node)],
type = gsub("I\\{", "", type)
)
all_types <- sort(unique(tip_info$type))
if (length(all_types) == 0) {
next
}
patristic_dist <- cophenetic.phylo(tree_phylo)
tree_file <- "/Users/lukelyu/Desktop/mySimulation/multi/83_beast2.trees"
tree <- read.beast(tree_file)
library(treeio)
library(ape)
library(dplyr)
tree <- read.beast(tree_file)
file_prefix <- sub("_beast2\\.trees$", "", basename(tree_file))
if (is.null(names(tree))) {
tree <- list(STATE_0 = tree)
}
get_pairwise_distances <- function(type1, type2, tip_info, patristic_dist) {
tips_type1 <- tip_info$tip_label[tip_info$type == type1]
tips_type2 <- tip_info$tip_label[tip_info$type == type2]
if (length(tips_type1) == 0 || length(tips_type2) == 0) {
return(numeric(0))
}
if (type1 == type2 && length(tips_type1) > 1) {
idx <- combn(length(tips_type1), 2)
patristic_dist[cbind(tips_type1[idx[1,]], tips_type1[idx[2,]])]
} else if (type1 != type2) {
as.vector(patristic_dist[tips_type1, tips_type2, drop = FALSE])
} else {
numeric(0)
}
}
all_csv_data <- list()
names(tree)
tree_file <- "/Users/lukelyu/Desktop/mySimulation/multi/83_beast2.trees"
tree <- read.beast(tree_file)
